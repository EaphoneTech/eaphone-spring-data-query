# .cnb.yml
master:
  push:
    - stages:
        - name: webhook-trigger
          jobs:
            - name: send-webhook
              script: |
                curl --header "Content-Type: application/json" --request POST --data "{}" http://flow-openapi.aliyun.com/pipeline/webhook/InMHPgZlAHDDVQdSa27W
